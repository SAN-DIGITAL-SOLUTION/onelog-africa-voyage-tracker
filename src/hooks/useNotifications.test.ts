import { renderHook, act } from '@testing-library/react';
import { useNotifications } from './useNotifications';

describe('useNotifications', () => {
  it('initialise et gère les notifications', async () => {
    // Mock supabase
    // ...
    // Teste la logique du hook (exemple)
    // ...
    expect(true).toBe(true); // À compléter selon la logique réelle
  });
});
