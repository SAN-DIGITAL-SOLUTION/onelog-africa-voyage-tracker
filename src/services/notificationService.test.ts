import { sendNotification } from './notificationService';

describe('sendNotification', () => {
  it('envoie une notification et log', async () => {
    // Mock supabase et préférences
    // ...
    expect(true).toBe(true); // À compléter selon la logique réelle
  });
});
