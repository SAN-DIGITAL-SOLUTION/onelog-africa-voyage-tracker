import { retryUnconfirmedNotifications } from '@/services/notificationRetryService';

describe('notificationRetryService', () => {
  it('relance les notifications non lues', async () => {
    // Mock supabase et sendNotification
    // ...
    expect(true).toBe(true); // À compléter selon la logique réelle
  });
});
